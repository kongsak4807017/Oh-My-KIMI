fix: use stdin mode for complex prompts on Windows

- Detect when content has spaces on Windows platform
- Use --input-format text with stdin instead of -p flag
- Fixes argument parsing issues with spaces in Thai text
