fix: keep REPL alive on Windows and replace emoji with text

- Add Promise-based wait loop to keep process alive until REPL closes
- Replace Unicode emoji with text labels for Windows compatibility
- Fix stdin/stdout handling on Windows CMD/PowerShell
