fix: update CLI provider for Kimi CLI v1.24+ syntax

- Change from 'kimi chat' to 'kimi --print --final-message-only -p'
- Kimi CLI v1.24+ uses different argument structure
- Add --final-message-only for clean text output
- Remove stdin piping, use -p flag instead
- Increase timeout to 120s for complex tasks
