feat: add real-time activity logging and extend timeout

- Add ActivityLogger with spinner animations and progress tracking
- Show real-time activities: thinking, reading, writing, commands
- Extend CLI timeout from 60s to 10 minutes for complex tasks
- Log chunk progress every 50 chunks
- Detect task type from input (repository, debugging, planning, etc.)
